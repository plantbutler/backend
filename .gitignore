# The token and the NAS address never enter the repository.
deploy.env
*.db
*.db-wal
*.db-shm
.venv/
__pycache__/
.pytest_cache/
# coverage.py's run file: this checkout's absolute paths, rewritten every run.
.coverage

# A local run stores its photographs beside its database (BUTLER_PHOTOS).
photos/
