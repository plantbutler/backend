[project]
name = "plantbutler-backend"
version = "0.13.0"
description = "Plant Butler backend: raw readings land here, commands leave from here"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.115",
  "uvicorn>=0.30",
]

[dependency-groups]
dev = [
  "pytest>=8",
  "httpx>=0.27",
]
